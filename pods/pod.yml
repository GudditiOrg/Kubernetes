apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  labels:
    app: nginx-pod

spec:
  containers:
    - name: nginx-pod
      image: gudditi/nginx-pod
      imagePullPolicy: IfNotPresent
      ports:
        - containerPort: 80


